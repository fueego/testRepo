git diff: pokazuje roznice pomiedzy plikami (workplace and staging(index));
git diff --chached: pokazuje roznice pomiedzy plikami (stage and repository);
git branch: pokazuje jakie mamy dostepne branche;
git checkout repo: zmienia bierzące repozytorium do repozytorium o nazwie "repo";